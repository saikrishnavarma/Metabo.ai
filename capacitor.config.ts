import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gorantlasaikrishnavarma.metaboai',
  appName: 'Metabo.ai',
  webDir: 'dist',
  ios: {
    contentInset: 'automatic',
  },
};

export default config;
